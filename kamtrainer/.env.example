# Copy this file to .env.local and fill in your Supabase project config
# (Supabase dashboard -> Project settings -> API).
# .env.local is gitignored, so your keys never get committed.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
